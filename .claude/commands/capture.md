---
description: Add a task to Todoist from a one-line description
---

Add to Todoist: $ARGUMENTS

Infer project, priority and due date from context and the conventions in
AGENTS.md. Use the `Action` label if it is a real next action. Check
`td task add --help` rather than guessing flags, and `--dry-run` if unsure.
Show the command, then run it. One task unless it is clearly several.
