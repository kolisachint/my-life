---
description: Append a fact to long-term memory
---

Append to `memory/learned.txt`: $ARGUMENTS

One line, format `YYYY-MM-DD | fact`, at the end of the file. Keep it specific and
actionable — a preference, a gotcha, a correction, something that would change how
a future session behaves. Skip it if it merely restates what AGENTS.md already
says. Then commit.
