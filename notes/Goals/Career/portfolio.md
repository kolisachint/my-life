# Open-source portfolio

_Generated by `bin/portfolio` on 2026-08-29. Do not hand-edit —
edit `data/portfolio-repos.txt` and re-run._

This is the evidence behind the **AI** half of his Data & AI positioning.
Before claiming he lacks proof of anything, read this file.

## hoocode

`github.com/kolisachint/hoocode` · last push 2026-08-28 · ts:898 (source files by extension)

HooCode is a terminal coding agent that keeps you in control: every edit and
shell command passes through a permission gate, and the agent is scoped by an
explicit mode (Ask · Plan · Build · Debug) instead of one do-everything prompt.

## embeddingsearchtools

`github.com/kolisachint/embeddingsearchtools` · last push 2026-08-08 · rs:13 ts:1 (source files by extension)

A minimal, modular embedding search engine in Rust. It generates embeddings and
serves low-latency similarity search behind a clean library API, a CLI, and a
long-lived stdio daemon designed to be driven from a TypeScript `spawn`.

## voicetools

`github.com/kolisachint/voicetools` · last push 2026-07-01 · rs:11 ts:1 (source files by extension)

Local, offline **voice-to-text for the terminal**. A small Rust binary that
captures your microphone, stops automatically when you go quiet, and streams
recognized text on stdout using a simple line protocol — designed to drop into
a TUI (e.g. hoocode) as a push-to-talk input.

## webtools

`github.com/kolisachint/webtools` · last push 2026-08-09 · rs:34 (source files by extension)

A unified, **token-efficient** web `fetch` + `search` CLI for LLM agents,
built around **reference-style URL preservation**. One small, fast binary that
works with no API keys and no backend — and takes them when you want better
search.

## browsertools

`github.com/kolisachint/browsertools` · last push 2026-06-28 · rs:15 py:3 (source files by extension)

A deterministic browser engine with **no LLM in-process**. It drives Chromium
over raw CDP (via `chromiumoxide`) and exposes three things:

## computertools

`github.com/kolisachint/computertools` · last push 2026-06-27 · — (source files by extension)


## filetools

`github.com/kolisachint/filetools` · last push 2026-06-27 · rs:25 (source files by extension)

Reversible, token-efficient file serialization for LLMs. The `filetools` CLI
extracts a file to editable JSON and reconstructs it after edits — **losslessly**.

## hooteams

`github.com/kolisachint/hooteams` · last push 2026-06-25 · ts:71 tsx:13 (source files by extension)

Multi-agent team orchestration on top of [hoocode](https://github.com/kolisachint/hoocode) agents, plus an SSE bridge that exposes the live team event stream to any client — the `hooteams` CLI, the built-in web UI, or any consumer of the wire format.

## hoocowork

`github.com/kolisachint/hoocowork` · last push 2026-06-14 · ts:231 tsx:213 (source files by extension)

A unified web interface for AI coding agents. Run **HooCode**, **Claude Code**, and **GitHub Copilot** from one place — locally or remotely.

## hoocanvas

`github.com/kolisachint/hoocanvas` · last push 2026-06-14 · ts:5 tsx:9 (source files by extension)

Live mission control for a [hooteams](https://github.com/kolisachint/hooteams) agent team. Pure frontend — consumes the hooteams SSE stream and renders one terminal pane per agent: streaming tokens with a blinking cyan cursor, collapsible thinking traces, tool lifecycle chips, and a `nudge >` input that steers any agent mid-run.

## pycortex

`github.com/kolisachint/pycortex` · last push 2026-07-31 · ts:4 py:363 (source files by extension)


## cortexcode

`github.com/kolisachint/cortexcode` · last push 2026-07-23 · rs:115 py:3 (source files by extension)

Rust migration of the [HooCode](https://github.com/kolisachint/hoocode) TypeScript coding-agent framework.

## product-builder

`github.com/kolisachint/product-builder` · last push 2026-06-14 · ts:15 tsx:12 (source files by extension)


## BrainDock

`github.com/kolisachint/BrainDock` · last push 2026-03-16 · ts:9 py:47 (source files by extension)

A **local-first second-brain assistant** that connects [Obsidian](https://obsidian.md) and Apple Reminders.

